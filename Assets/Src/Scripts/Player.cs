using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Player : S_Parent
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log(game.audioManager.name);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
